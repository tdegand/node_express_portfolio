extends layout.pug

block content 

    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.error-container
            h3.error-message #{err.message} #{err.status}
            p.error-stack #{err.stack}